# the package README is copied from the root dir; hand-written subdir READMEs
# (src/types, cts-keep, ...) stay tracked
/README.md

# generated html documentation
docs

# cloned test fixtures (bevy shaders, etc.)
fixtures

# nodebug build output (for testing against prod builds)
dist-nodebug